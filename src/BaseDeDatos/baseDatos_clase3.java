/*package baseDatos;

import java.io.RandomAccessFile;
import java.sql.SQLException;
import java.util.Scanner;

public class baseDatos_clase3 {
	public static void main(String[] args) {

		File tren = new File;
		File reserva = new File;
		
		RandomAccessFile trenes = new Ra;
				
		RandomAccesFile recervas = new Ra;
		
		do
			codigo = leerCadena(trenes,5);
			leerCadena (trenes,25);
			cap=trenes.readShort();
			
			reservas.seek(0);
			encontrado = false;
			ocupadas=0;
			
			do {
				codigoR = leerCadena(reservas,5);
				ocupadas = reseras.readShort();
				if codigoR.equals(codigoT)
					encontrado=true;
						
			} while !encontrado && true
			
			SOP (codigoR, cap-ocupadas)
			
	} while (true)
}

*/
